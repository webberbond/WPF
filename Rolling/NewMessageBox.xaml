<Window x:Class="Rolling.NewMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:Rolling"
        mc:Ignorable="d"
        Title="Login" Height="300" Width="400"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{x:Null}"
        AllowsTransparency="True"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen">
    <materialDesign:Card UniformCornerRadius="15" Background="{DynamicResource MaterialDesignPaper}" Margin="7"
                         materialDesign:ShadowAssist.ShadowDepth="Depth4">
            <materialDesign:DialogHost CloseOnClickAway="True" x:Name="DialogHost">
                <StackPanel Margin="10">
                    <TextBlock x:Name="ErrorText" Text="Error Text" FontSize="25" HorizontalAlignment="Center"/>
                <Button Content="ОК" VerticalAlignment="Bottom" HorizontalAlignment="Center" Height="30" Width="100" Margin="0,200,0,0" Click="Button_Click"/>
            </StackPanel>
        </materialDesign:DialogHost>
        </materialDesign:Card>
</Window>
